{
  "name": "companion-module-eyevinn-open-live",
  "version": "1.0.0",
  "description": "Bitfocus Companion v3 module for Open Live broadcast production platform",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node18 --outfile=dist/main.js",
    "dev": "esbuild src/main.ts --bundle --platform=node --target=node18 --outfile=dist/main.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": "^18.12 || ^22.8"
  },
  "dependencies": {
    "@companion-module/base": "^1.12.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "@types/ws": "^8.5.14",
    "esbuild": "^0.28.0",
    "typescript": "~5.7.3"
  }
}
